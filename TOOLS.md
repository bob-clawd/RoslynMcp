# Tool descriptions

| Tool | Description | Parameters | Outputs |
| ---- | ----------- | ---------- | ------- |
| `load_solution` | Use this tool when you need to start working with a .NET solution and no solution has been loaded yet. This must be the first tool called in a session before any code analysis or navigation tools can be used. | `solutionHintPath` (optional): Absolute path to a `.sln` file, or to a directory used as the recursive discovery root for `.sln`/`.slnx` files. If omitted, the tool auto-detects from the current workspace. | Loaded solution path plus `projects` and `edges` summaries. `projects.count` is the number of projects actually emitted in the role buckets, and each emitted project carries only `references` / `referencedBy` counts. `edges.references` contains the explicit project-reference edge list for projects with stable paths. |
| `load_project` | Use this tool when you need to list types declared in a specific project. It is useful for project-scoped discovery, for finding type symbols before follow-up calls such as `load_type` or `load_member`. | `projectPath`: Exact path to a project file (`.csproj`), obtained from `load_solution`. | Type count plus project-local types, with each type entry and its member count. |
| `load_type` | Use this tool when you need to inspect type hierarchy and members declared by the specific type. | `typeSymbolId`: The stable symbol ID of a type, obtained from `load_project` or from a `search_type` match. | The resolved type symbol, optional documentation summary, derived types, implementations, and declared handwritten members. |
| `load_member` | Use this tool when you need callers/callees or overrides/implementations of a symbol. | `memberSymbolId`: The stable symbol ID, obtained from `load_type` or from a `search_member` match. | The resolved member symbol, documentation, direct callers, direct callees, overrides, and implementations. |
| `search_member` | Use this tool when you only have a member name fragment (method/property/event/ctor). It performs a fast syntax-only search over the loaded solution. If exactly one match is found, it automatically returns the `load_member` result for that symbol. | `query`: Member name fragment to search for (case-insensitive contains match). | If ambiguous: returns a capped list of matches with `fullName`, `projectPath`, `location`, `kind`, optional `signature`, and `memberSymbolId` for up to the first 10 matches. When omitted, the id was not semantically resolved. When the list is cut off, the response also includes `truncated: true`. If unique: returns an embedded `load_member` result. |
| `run_tests` | Default .NET test runner. Use this instead of `dotnet test` unless you need unsupported CLI behavior. | `target` (optional): Execution target. Omit to run the currently loaded solution. Supports solution-relative or absolute `.sln`, `.slnx`, `.csproj`, or directory paths when the resolved target stays within the loaded solution directory.<br><br>`filter` (optional): `dotnet test` filter expression, passed through where practical. | Test run outcome, exit code, summary, grouped test failures, optional build diagnostics, and aggregated test counts. |
| `search_type` | Fast, syntax-only type discovery across the loaded solution. Use this when you only have a type name fragment and want to avoid grep/glob. If exactly one match is found, it automatically returns the `load_type` result for that type. | `query`: Type name fragment (case-insensitive contains match). | If exactly one match: `type` contains a `load_type` result. If multiple matches: returns a capped `matches` list with `fullName`, `projectPath`, and `typeSymbolId` for up to the first 10 matches. When omitted, the id was not semantically resolved. When the list is cut off, the response also includes `truncated: true`. |
| `diagnose_file` | Get compiler error diagnostics for one source file without reloading the solution or running tests. | `filePath`: Path to a source file within the currently loaded solution. Accepts an absolute path or a path relative to the workspace. | Up to 10 file-local compiler errors with `line`, `column`, `id`, and `message`. |

# System Prompt

```
For C# code, Prefer the `roslyn` tools over bash
```
